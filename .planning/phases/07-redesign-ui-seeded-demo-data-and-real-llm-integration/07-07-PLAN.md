---
phase: 07-redesign-ui-seeded-demo-data-and-real-llm-integration
plan: 07
type: execute
wave: 4
depends_on: ["07-05", "07-06"]
files_modified:
  - web/src/screens/SignIn.tsx
  - web/src/screens/NotBuiltYet.tsx
  - web/src/screens/NotFound.tsx
  - e2e/sign-in.spec.ts
  - docs/a11y/sign-in.md
  - docs/carbon-conformance-register.md
autonomous: true

features:
  implements: ["F2"]
  depends_on: []
  enables: []

must_haves:
  truths:
    - "The sign-in screen, the transitional NotBuiltYet placeholder, and the NotFound screen all render on Carbon via the already-rebuilt shared form/state components (07-06), with zero USWDS class remaining in any of the three files"
    - "The sign-in screen's deliberate-absence rules (no remember-me, no credential recovery, no SSO, no field-level 'which one was wrong' leak) survive the rebuild unchanged"
    - "docs/a11y/sign-in.md is re-signed against the rebuilt screen"
  artifacts:
    - path: "web/src/screens/SignIn.tsx"
      provides: "The F1 sign-in screen, rendered via the Carbon-based Field/SubmitButton/UswdsForm/ErrorSummary from 07-06, unchanged failure-handling logic"
  key_links:
    - from: "web/src/screens/SignIn.tsx"
      to: "web/src/components/UswdsForm.tsx (07-06)"
      via: "import { Field, SubmitButton, UswdsForm } from '../components/UswdsForm.js' — unchanged import"
      pattern: "from '../components/UswdsForm.js'"

integration_contracts:
  requires:
    - from_plan: "07-06"
      artifact: "web/src/components/UswdsForm.tsx"
      exports: ["Field", "SubmitButton", "UswdsForm"]
      verify: "grep -q 'export function Field' web/src/components/UswdsForm.tsx && echo CONTRACT_OK"
    - from_plan: "07-06"
      artifact: "web/src/components/ErrorSummary.tsx"
      exports: ["ErrorSummary"]
      verify: "grep -q 'export function ErrorSummary' web/src/components/ErrorSummary.tsx && echo CONTRACT_OK"
    - from_plan: "07-05"
      artifact: "web/src/shell/Shell.tsx"
      exports: ["Shell"]
      verify: "grep -q 'export function Shell' web/src/shell/Shell.tsx && echo CONTRACT_OK"
  provides:
    - artifact: "web/src/screens/SignIn.tsx"
      exports: ["SignIn"]
      shape: "export function SignIn(): JSX.Element — unchanged signature, consumed unchanged by web/src/app/router.tsx"
      verify: "grep -q 'export function SignIn' web/src/screens/SignIn.tsx && echo CONTRACT_OK"
---

<objective>
Rebuild the three smallest, structurally-simplest screens — `SignIn`
(F1/F2), the transitional `NotBuiltYet` placeholder (rendered at
`/entries/new` until F6 exists), and `NotFound` (the `*` route) — on top of
the already-Carbon-rebuilt shell (07-05) and shared component library
(07-06). None of these screens introduces any new pattern beyond what
07-06 already delivered; this plan is a pure consumption exercise, proving
the shared-component contract holds.

Purpose: retire USWDS from the authentication entry point and the two
fallback screens.
Output: three screens rendering entirely via Carbon-based shared components;
`docs/a11y/sign-in.md` re-signed; `e2e/sign-in.spec.ts` updated.
</objective>

<feature_dependencies>
Implements: F2: USWDS Application Shell & Accessibility Foundation, on F1's
sign-in screen and the shell's fallback screens.
Depends on: None as a tracked feature ID.
Enables: None as a tracked feature ID.
</feature_dependencies>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@project_specs/FRD/F02-uswds-shell-accessibility.md
@project_specs/UX-Mockup/Y0-patterns.md

@web/src/screens/SignIn.tsx
@web/src/screens/NotBuiltYet.tsx
@web/src/screens/NotFound.tsx
@e2e/sign-in.spec.ts
@docs/a11y/sign-in.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild SignIn.tsx on Carbon; re-sign its a11y record</name>
  <files>web/src/screens/SignIn.tsx, docs/a11y/sign-in.md, docs/carbon-conformance-register.md</files>
  <action>
Rebuild `SignIn.tsx` using ONLY the already-Carbon-rebuilt `Field`,
`SubmitButton`, `UswdsForm` (from `web/src/components/UswdsForm.tsx`) and
`ErrorSummary` (from `web/src/components/ErrorSummary.tsx`) — their imports
are UNCHANGED from before this plan, since 07-06 preserved every export
name and prop shape. Replace the remaining USWDS-specific markup in this
file: the `grid-row`/`grid-col-12 tablet:grid-col-6 desktop:grid-col-4`
layout classes become Carbon `Grid`/`Column` (verify Carbon's responsive
column-span prop names for an equivalent tablet/desktop breakpoint split);
the three `usa-alert` variants (session-expired info, signed-out success,
generic failure — note the failure alert is ALREADY `ErrorSummary`, already
handled by 07-06) that remain inline in this file (the "Your session
expired…" info alert and the "You are signed out." success alert) become
Carbon `InlineNotification` (`kind="info"` / `kind="success"`
respectively), preserving `role="status"` and their exact wording verbatim.

Preserve EVERY behavioural/logic detail unchanged — this plan touches
presentation only:
- Exactly one generic failure summary item on invalid credentials; neither
  input gets `aria-invalid` in that case (no field-level leak).
- On any failure: password cleared, email retained.
- The 422 `REQUEST_MALFORMED` per-field branch (the ONLY case that marks a
  field invalid) is untouched logic — only the rendering of `Field`'s
  `invalid`/`errorText` props (already Carbon-based via 07-06) changes.
- The `arrivedExpired`/`arrivedSignedOut` announcement timing and text.
- Deliberate absences: no remember-me, no credential-recovery link, no
  SSO/PIV button, no password-visibility toggle, no role selector.

Update `e2e/sign-in.spec.ts`: replace `a.usa-skipnav` and
`[role="alert"].usa-alert--error` locators with the Carbon-rendered
equivalents (prefer `page.getByRole('alert')` over a class-name selector
where the assertion is really "an alert exists," reserving a class-name
locator only where the test genuinely needs to assert exactly which visual
variant rendered).

Re-sign `docs/a11y/sign-in.md`: re-run its full checklist against the
rebuilt screen, cite the updated `e2e/sign-in.spec.ts` test names, record a
new reviewer/date/commit, and note any defect + resolution. Append a row to
`docs/carbon-conformance-register.md` for the sign-in form and its two
inline notices.
  </action>
  <verify>
```bash
npm run build
npm run typecheck
npx playwright test e2e/sign-in.spec.ts --reporter=list 2>&1 | tail -40
echo "SIGNIN PLAYWRIGHT PASSED"
```
  </verify>
  <done>
`SignIn.tsx` renders entirely via Carbon (through the unchanged 07-06
imports plus Carbon `Grid`/`InlineNotification` for its own remaining
markup). All `e2e/sign-in.spec.ts` scenarios pass (0 failing, 0 skipped),
covering: field input, invalid-credentials generic failure with password
cleared/email retained, the 422 per-field path, the expired/signed-out
inline notices, and busy-state double-submit blocking. `docs/a11y/sign-
in.md` is re-signed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild NotBuiltYet.tsx and NotFound.tsx on Carbon</name>
  <files>web/src/screens/NotBuiltYet.tsx, web/src/screens/NotFound.tsx, docs/carbon-conformance-register.md</files>
  <action>
Both screens are small and structurally identical (an `h1`, one
`InlineNotification`/prose paragraph, and a link). Rebuild `usa-prose` →
Carbon typography utility classes, and the `usa-alert usa-alert--info`
(`NotBuiltYet`) → Carbon `InlineNotification kind="info"`, `role="status"`.
`NotFound` has no alert (plain prose + link) — only its layout classes
change.

Preserve unchanged: `useScreenFocus` wiring (title announcement + `h1`
focus, both from `web/src/shell/useScreenFocus.ts`, untouched by this
plan), the exact deliberate-absence constraints on `NotBuiltYet` (no
disabled control, no "coming soon" chip, no tooltip), and the "link to the
OTHER nav destination" logic in `NotBuiltYet` (reads `NAV_ITEMS`, unchanged
since 07-05).

Append rows to `docs/carbon-conformance-register.md` for the `NotBuiltYet`
placeholder notice and the `NotFound` screen.
  </action>
  <verify>
```bash
npm run build
npm run typecheck
npx vitest run server/test/architecture/navigation.spec.ts --reporter=default
echo "NOTBUILTYET/NOTFOUND OK"
```
  </verify>
  <done>
`NotBuiltYet.tsx` and `NotFound.tsx` render entirely on Carbon with no
`usa-*` class remaining. `navigation.spec.ts`'s route-table assertion
(unaffected by this plan) still passes. Both screens still correctly focus
their `h1` and announce their title on mount (unchanged `useScreenFocus`
behaviour).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → SignIn submission | Email/password cross from the sign-in form into `signIn()`; the server response is authoritative (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Information disclosure | `SignIn.tsx`'s field-level-error suppression on credential failure | mitigate | Unchanged logic: `viewForError` only marks a field invalid on the 422 `REQUEST_MALFORMED` path; every other failure (401/429/network) renders one generic, fieldless summary item — re-verified unmodified by this plan's own `e2e/sign-in.spec.ts` run, not just asserted by code inspection |
| T-07-21 | Tampering | Carbon `InlineNotification` rendering the session-expired/signed-out reason text | mitigate | The reason string is read from a fixed `URLSearchParams` allowlist (`'expired'`/`'signed-out'`) already in the pre-existing logic (unchanged by this plan), never rendered as raw arbitrary query content |

</threat_model>

<verification>
- `e2e/sign-in.spec.ts` (13 scenarios per `docs/a11y/shell.md`'s prior
  evidence set) passes fully against the Carbon rebuild.
- `docs/a11y/sign-in.md` is re-signed.
- No behavioural/logic change to `SignIn.tsx`'s failure-handling, only
  presentation.
- `NotBuiltYet`/`NotFound` retain their focus-management and deliberate-
  absence guarantees.
</verification>

<success_criteria>
- Three of the eighteen USWDS-coupled files are now fully Carbon-rendered
  with zero behavioural regression.
- The shared-component contract from 07-05/07-06 is proven to require no
  consumer-side prop changes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-redesign-ui-seeded-demo-data-and-real-llm-integration/07-07-SUMMARY.md`
</output>
</content>
