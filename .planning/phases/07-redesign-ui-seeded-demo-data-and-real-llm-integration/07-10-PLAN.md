---
phase: 07-redesign-ui-seeded-demo-data-and-real-llm-integration
plan: 10
type: execute
wave: 5
depends_on: ["07-09"]
files_modified:
  - web/src/components/DecisionPanel.tsx
  - web/src/components/AuditTrailRegion.tsx
  - e2e/case-detail.spec.ts
  - e2e/decision.spec.ts
  - e2e/audit-trail.spec.ts
  - docs/a11y/case-detail.md
  - docs/carbon-conformance-register.md
autonomous: true

features:
  implements: ["F2"]
  depends_on: []
  enables: []

must_haves:
  truths:
    - "DecisionPanel's three decision actions (Approve/Edit/Reject) carry EQUAL visual weight via one shared Carbon Button kind, with no pre-selected default and no primary/secondary visual steer, on Carbon"
    - "The two-step commitment pattern (choose -> complete -> review -> record) still uses a visible step indicator (Carbon ProgressIndicator/ProgressStep) and still requires a deliberate second action to record a decision"
    - "AuditTrailRegion's value-change table still renders per-value ProvenanceBadge on both sides of every before/after row, states the after-origin in words, and offers no repair/export/mutation affordance on a tampered-chain integrity failure"
    - "docs/a11y/case-detail.md is re-signed as a JOINT record covering all five case-detail sections (header/nav/entry/findings/recommendation from 07-09, plus decision/audit from this plan) — the sign-off is not considered complete until this plan closes it"
  artifacts:
    - path: "web/src/components/DecisionPanel.tsx"
      provides: "The F12 decision region on Carbon: chooser -> edit/reject form -> summary -> confirmation, using the 07-06 Carbon form primitives and ProvenanceBadge unchanged"
    - path: "web/src/components/AuditTrailRegion.tsx"
      provides: "The F14 read-only audit trail on Carbon: event list, before/after value table with per-cell provenance, integrity-failure alert with no repair action"
  key_links:
    - from: "web/src/components/DecisionPanel.tsx"
      to: "web/src/components/UswdsForm.tsx, web/src/components/ErrorSummary.tsx, web/src/components/ProvenanceBadge.tsx (07-06, unchanged)"
      via: "existing imports, unchanged prop usage"
      pattern: "from '../components/(UswdsForm|ErrorSummary|ProvenanceBadge).js'"

integration_contracts:
  requires:
    - from_plan: "07-09"
      artifact: "web/src/screens/CaseDetail.tsx"
      exports: ["CaseDetail"]
      verify: "grep -q 'export function CaseDetail' web/src/screens/CaseDetail.tsx && echo CONTRACT_OK"
    - from_plan: "07-06"
      artifact: "web/src/components/ProvenanceBadge.tsx"
      exports: ["ProvenanceBadge"]
      verify: "grep -q \"origin: 'AI' | 'HUMAN'\" web/src/components/ProvenanceBadge.tsx && echo CONTRACT_OK"
    - from_plan: "07-06"
      artifact: "web/src/components/UswdsForm.tsx"
      exports: ["TextAreaField", "SubmitButton", "UswdsForm"]
      verify: "grep -q 'export function TextAreaField' web/src/components/UswdsForm.tsx && echo CONTRACT_OK"
  provides:
    - artifact: "web/src/components/DecisionPanel.tsx"
      exports: ["DecisionPanel"]
      shape: "Unchanged props (consumed by CaseDetail.tsx, itself unchanged by this plan)"
      verify: "grep -q 'export function DecisionPanel' web/src/components/DecisionPanel.tsx && echo CONTRACT_OK"
    - artifact: "web/src/components/AuditTrailRegion.tsx"
      exports: ["AuditTrailRegion"]
      shape: "Unchanged props (consumed by CaseDetail.tsx, itself unchanged by this plan)"
      verify: "grep -q 'export function AuditTrailRegion' web/src/components/AuditTrailRegion.tsx && echo CONTRACT_OK"
---

<objective>
Rebuild the last two USWDS-coupled files — `DecisionPanel.tsx` (F12) and
`AuditTrailRegion.tsx` (F14), both rendered as children inside `CaseDetail`
(07-09) — on Carbon. This completes the Carbon migration of every screen in
the product. Because these two regions together with 07-09's sections make
up the FULL case-detail screen, this plan is also where
`docs/a11y/case-detail.md`'s per-screen sign-off is CLOSED as a single
joint record covering all five sections — it was deliberately left open by
07-09.

The decision region's load-bearing design constraint is EQUAL WEIGHT across
Approve / Edit-and-approve (or Resolve-directly) / Reject: FR-12.1 forbids
any visual steer toward one action, so all three must render with the SAME
Carbon `Button` `kind`, never a mix of primary/secondary/tertiary. The two-
step commitment pattern (Pattern 3) needs a visible step indicator, mapped
to Carbon's `ProgressIndicator`/`ProgressStep`.

Purpose: complete the eighteen-file USWDS-to-Carbon migration and close the
final per-screen accessibility sign-off.
Output: `DecisionPanel.tsx` and `AuditTrailRegion.tsx` rendering on Carbon;
`docs/a11y/case-detail.md` closed as a joint five-section record;
`e2e/decision.spec.ts`, `e2e/audit-trail.spec.ts`, and the remaining
`e2e/case-detail.spec.ts` locators updated.
</objective>

<feature_dependencies>
Implements: F2: USWDS Application Shell & Accessibility Foundation, on
F12's decision region and F14's audit-trail region — completing the Carbon
migration of every screen.
Depends on: None as a tracked feature ID.
Enables: None as a tracked feature ID.
</feature_dependencies>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@project_specs/FRD/F02-uswds-shell-accessibility.md
@project_specs/TechArch/07-accessibility.md
@project_specs/UX-Mockup/Y0-patterns.md
@docs/uswds-conformance-register.md

@web/src/components/DecisionPanel.tsx
@web/src/components/AuditTrailRegion.tsx
@web/src/screens/CaseDetail.tsx
@e2e/decision.spec.ts
@e2e/audit-trail.spec.ts
@e2e/case-detail.spec.ts
@docs/a11y/case-detail.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild DecisionPanel.tsx on Carbon — equal-weight actions, two-step commitment</name>
  <files>web/src/components/DecisionPanel.tsx, e2e/decision.spec.ts, docs/carbon-conformance-register.md</files>
  <action>
Read the full current `DecisionPanel.tsx` (764 lines) before editing.
Rebuild its state machine's PRESENTATION only — the chooser, the edit/reject
form, the pre-submission summary, and the server-response-driven
confirmation — on Carbon, with zero change to the underlying logic
(permitted-decisions gating, the ≥10-char reason requirement, the
trim-then-byte-compare changed-field detection, the idempotency-key
minting, the complete-field-set submission on edit, the 409-conflict
handling).

**The three decision actions — the single most load-bearing visual
constraint in this file.** Render Approve / Edit-and-approve (or Resolve-
directly) / Reject as three Carbon `Button` components sharing EXACTLY ONE
`kind` value (e.g. all `kind="tertiary"`, or all `kind="secondary"` — pick
ONE and use it identically on all three; verify no default styling
difference sneaks in from render order or conditional classes). No
`kind="primary"` on any of them, no pre-selection, no `autoFocus`. Preserve
DOM order Approve → Edit → Reject and the "Approve absent + stated note
when not permitted" behaviour. `e2e/decision.spec.ts`'s existing test "the
three buttons share exactly one class" needs re-pointing at whatever
Carbon's actual rendered class for that shared `kind` turns out to be — the
ASSERTION (equal weight, one shared class) stays; only the literal class
string it checks changes.

**Two-step commitment (Pattern 3)** → Carbon `ProgressIndicator` +
`ProgressStep` for "Choose an action → Complete the form → Review → Record"
(map the exact step count/labels this project's mockup specifies — verify
against `Y0-patterns.md` Pattern 3's diagram before deciding the step
count). The commit/record button remains the ONLY button in the whole path
that could be argued "primary" in intent — even so, verify against FR-12.2/
Pattern 3 whether it should still be visually distinct or whether equal-
weight applies here too; if the existing implementation already treats it
distinctly, preserve that distinction on Carbon (e.g. `kind="primary"`
ONLY on the final record/commit action, never on the three choice
buttons) — do not silently change which button is visually primary.

**Edit/reject form** → the SAME shared `TextAreaField`/`Field`/`UswdsForm`/
`ErrorSummary` imports from `web/src/components/*` (07-06), UNCHANGED. The
"Specialist-modified" marking on each changed field uses `ProvenanceBadge`'s
existing `modified` prop, imported unchanged.

**Pre-submission summary / confirmation panels** → plain Carbon typography
composition (`dl`/`dt`/`dd` or Carbon's list primitives), each value's
origin shown via the unchanged `ProvenanceBadge`. The confirmation is built
ONLY from the server's `DecisionRecordResponse` — unchanged.

Update `e2e/decision.spec.ts`'s `.usa-alert--error`/`.usa-tag`/class-based
locators to the Carbon-rendered equivalents. Append the three-button
equal-weight composition, the edit-resolution form (reusing the registered
form pattern), the pre-submission summary, and the confirmation panel to
`docs/carbon-conformance-register.md`, mirroring the retired register's own
emphasis on WHY the equal-weight composition matters (FR-12.1).
  </action>
  <verify>
```bash
npm run build
npm run typecheck
npx playwright test e2e/decision.spec.ts --reporter=list 2>&1 | tail -60
echo "DECISION PLAYWRIGHT PASSED"
```
  </verify>
  <done>
`DecisionPanel.tsx` renders entirely on Carbon. All 8 `e2e/decision.spec.ts`
scenarios pass (0 failing, 0 skipped), including the equal-weight-buttons
assertion (re-pointed at Carbon's actual class, same semantic check), the
two-step commitment flow, the ≥10-char reason gate, the specialist-modified
marking, and the 409-conflict presentation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild AuditTrailRegion.tsx on Carbon; close the joint case-detail.md sign-off</name>
  <files>web/src/components/AuditTrailRegion.tsx, e2e/audit-trail.spec.ts, e2e/case-detail.spec.ts, docs/a11y/case-detail.md, docs/carbon-conformance-register.md</files>
  <action>
Read the full current `AuditTrailRegion.tsx` (414 lines) before editing.
Rebuild the intro statement, the integrity statement (healthy `<p>` /
failed `role="alert"` naming the divergent sequence, FR-14.10), the
chronological `<ol>` of events (each with heading, `dl` Who/When/What-
changed, verbatim reason block), and the before/after value-change
`<table>` (caption + `scope="col"` headers) on Carbon — using Carbon's
`Table`/`TableHead`/`TableRow`/`TableHeader`/`TableBody`/`TableCell`
primitives for the table specifically (a genuine data table, NOT an ARIA
grid — same reasoning as the queue table in 07-08), and Carbon
`InlineNotification kind="error"` + `role="alert"` for the integrity-
failure alert.

Preserve unchanged: EVERY per-value cell's `ProvenanceBadge` (unchanged
import), the AI-actor branch rendering `AI ({model_id})` and never reading
`entry.actor`, a rejection's null after rendered "Not recorded (rejected)",
the after-origin stated in WORDS in the Origin column (not colour alone),
the "Event i of n" progress text, NO edit/correct/delete/print/download
affordance anywhere (FR-14 read-only-by-construction), and the live
post-decision in-place refresh via the `refreshToken`/`caseVersion` prop
threading from `CaseDetail` (unchanged by this plan — `CaseDetail.tsx` is
NOT touched in this task beyond nothing, since its prop-passing to
`AuditTrailRegion` is already stable from 07-09).

Update `e2e/audit-trail.spec.ts`'s class-based locators (`.usa-tag`, any
`usa-alert` reference) to Carbon's rendered equivalents. Update the
REMAINING `e2e/case-detail.spec.ts` locators that 07-09 explicitly deferred
(the decision/audit-region assertions named in 07-09's SUMMARY) — read that
SUMMARY first to know exactly which scenarios were left for this plan.

**Close the joint `docs/a11y/case-detail.md` sign-off.** This is the FIRST
point at which the ENTIRE case-detail screen (all five sections: header/
nav/entry/findings/recommendation from 07-09, decision from Task 1, audit
from this task) is fully Carbon-rendered. Re-run the COMPLETE §7.7
checklist against the whole screen — not just the two regions this plan
touched — citing the updated `e2e/case-detail.spec.ts`, `e2e/decision.
spec.ts`, and `e2e/audit-trail.spec.ts` test names, a new reviewer/date/
commit, and an explicit statement that all five sections are delivered
(mirroring the existing record's own "marking all five sections delivered"
convention from the pre-Phase-7 sign-off). Record any defect and its
resolution.

Append the audit value-change table composition and the integrity-failure
alert to `docs/carbon-conformance-register.md`, completing the register.
  </action>
  <verify>
```bash
npm run build
npm run typecheck
npx playwright test e2e/audit-trail.spec.ts e2e/case-detail.spec.ts e2e/decision.spec.ts --reporter=list 2>&1 | tail -80
echo "AUDIT/CASE-DETAIL/DECISION PLAYWRIGHT PASSED"
```
  </verify>
  <done>
`AuditTrailRegion.tsx` renders entirely on Carbon. All `e2e/audit-
trail.spec.ts` scenarios pass, including chronological order, per-value
provenance, the AI-never-a-person guarantee, the tampered-chain integrity
failure with no repair action, and the deep-link focus. The FULL
`e2e/case-detail.spec.ts` suite (every scenario, including the ones 07-09
deferred) now passes. `docs/a11y/case-detail.md` is re-signed as ONE joint
record covering all five sections, with a fresh reviewer/date/commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server decision/audit responses → DecisionPanel/AuditTrailRegion render | Decision confirmation and audit-trail data cross into these components; both remain strictly read-from-server, no client-side mutation of governed state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-27 | Elevation of privilege | DecisionPanel's equal-weight button constraint regressing (a visual nudge toward one action) | mitigate | `e2e/decision.spec.ts`'s "three buttons share exactly one class" assertion is re-pointed at Carbon's actual class, not removed — a visual-weight regression fails this specific, permanent test |
| T-07-28 | Tampering | AuditTrailRegion offering a repair/edit affordance on integrity failure | mitigate | FR-14's read-only-by-construction guarantee is unchanged code (no new button/input/link with a mutation affordance); `e2e/audit-trail.spec.ts`'s existing "no mutation control anywhere" assertion is re-verified against the Carbon rebuild, not skipped |
| T-07-29 | Repudiation | Audit event actor rendering (AI vs specialist) | mitigate | The AI-actor branch's `AI ({model_id})` rendering and its explicit avoidance of `entry.actor` for AI events is unchanged logic — re-verified by the existing e2e assertion against the Carbon-rendered table |

</threat_model>

<verification>
- `e2e/decision.spec.ts`, `e2e/audit-trail.spec.ts`, and the FULL
  `e2e/case-detail.spec.ts` all pass (0 failing, 0 skipped).
- `docs/a11y/case-detail.md` is re-signed as one joint record covering all
  five sections.
- `docs/carbon-conformance-register.md` is complete for every case-detail
  control.
- The equal-weight three-button constraint and the audit trail's
  read-only-by-construction guarantee are both structurally re-verified,
  not merely asserted by inspection.
</verification>

<success_criteria>
- All eighteen originally USWDS-coupled `.tsx` files are now Carbon-
  rendered (this plan closes the last two).
- The case-detail screen's per-screen accessibility sign-off is complete
  and current.
- Every screen-level e2e spec is green against the Carbon rebuild — the
  ONLY thing remaining before the phase's final cleanup wave is retiring
  USWDS itself from the dependency tree and build pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/07-redesign-ui-seeded-demo-data-and-real-llm-integration/07-10-SUMMARY.md`
</output>
</content>
