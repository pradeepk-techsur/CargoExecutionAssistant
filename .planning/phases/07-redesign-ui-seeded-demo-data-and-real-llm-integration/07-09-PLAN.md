---
phase: 07-redesign-ui-seeded-demo-data-and-real-llm-integration
plan: 09
type: execute
wave: 4
depends_on: ["07-05", "07-06"]
files_modified:
  - web/src/screens/CaseDetail.tsx
  - e2e/case-detail.spec.ts
  - docs/carbon-conformance-register.md
autonomous: true

features:
  implements: ["F2"]
  depends_on: []
  enables: []

must_haves:
  truths:
    - "CaseDetail's core reading-order sections (h1 header, On-this-page nav, submitted entry values, findings, AI recommendation) render on Carbon, with the fixed FR-10.10 reading order and every AI-recommendation state (PENDING/AVAILABLE/UNAVAILABLE/stale) unchanged"
    - "Every submitted-entry and AI-recommendation value still renders with its ProvenanceBadge adjacent, using the Carbon-rebuilt badge from 07-06, never colour alone"
    - "This plan does NOT close docs/a11y/case-detail.md's sign-off — the decision and audit-trail regions (DecisionPanel, AuditTrailRegion) are still USWDS-rendered stubs-in-progress until 07-10, so the joint five-section re-sign-off happens there, not here"
  artifacts:
    - path: "web/src/screens/CaseDetail.tsx"
      provides: "The F10 case-detail screen's header/nav/entry/findings/recommendation sections on Carbon, still rendering the (not-yet-migrated) DecisionPanel and AuditTrailRegion children unchanged via their existing props contract"
  key_links:
    - from: "web/src/screens/CaseDetail.tsx"
      to: "web/src/components/DecisionPanel.tsx, web/src/components/AuditTrailRegion.tsx (unchanged in THIS plan, migrated in 07-10)"
      via: "existing prop-based composition — this plan does not change either child component's props"
      pattern: "<DecisionPanel|<AuditTrailRegion"

integration_contracts:
  requires:
    - from_plan: "07-06"
      artifact: "web/src/components/ProvenanceBadge.tsx"
      exports: ["ProvenanceBadge"]
      verify: "grep -q \"origin: 'AI' | 'HUMAN'\" web/src/components/ProvenanceBadge.tsx && echo CONTRACT_OK"
    - from_plan: "07-06"
      artifact: "web/src/components/states.tsx"
      exports: ["Degraded", "ErrorState"]
      verify: "grep -q 'export function Degraded' web/src/components/states.tsx && echo CONTRACT_OK"
  provides:
    - artifact: "web/src/screens/CaseDetail.tsx"
      exports: ["CaseDetail"]
      shape: "export function CaseDetail(props: { focusAuditTrail?: boolean }): JSX.Element — unchanged signature, consumed unchanged by web/src/app/router.tsx; renders <DecisionPanel> and <AuditTrailRegion> with their EXISTING prop contract, unchanged by this plan"
      verify: "grep -q 'export function CaseDetail' web/src/screens/CaseDetail.tsx && echo CONTRACT_OK"
---

<objective>
Rebuild the F10 case-detail screen's core reading-order sections — the `h1`
header (reference, receipt time, state, back-to-queue link), the
unconditional "On this page" navigation, the submitted-entry values section,
the validation-findings section, and the AI-recommendation section (all four
presentations: PENDING/AVAILABLE/UNAVAILABLE/stale-PENDING) — on Carbon,
using the already-rebuilt `ProvenanceBadge` and shared state components
(07-06) and shell (07-05).

This plan deliberately does NOT touch `DecisionPanel.tsx` or
`AuditTrailRegion.tsx` (07-10's job) — `CaseDetail.tsx` continues to render
both as children with their EXISTING props, unchanged. This means the
screen is visually mixed (Carbon header/findings/recommendation, still-
USWDS decision/audit regions) for one wave — acceptable because the full
`npm run test:e2e` regression gate is deliberately deferred to the final
cleanup wave (07-11), and this plan's own verification only requires the
sections it actually rebuilt to behave correctly.

Purpose: retire USWDS from the single largest, most complex screen's
read-only reporting sections.
Output: `CaseDetail.tsx`'s core sections rendering on Carbon;
`e2e/case-detail.spec.ts` updated for the sections this plan touches.
</objective>

<feature_dependencies>
Implements: F2: USWDS Application Shell & Accessibility Foundation, on
F10's case-detail screen (core reading sections only — decision/audit
regions are 07-10).
Depends on: None as a tracked feature ID.
Enables: None as a tracked feature ID.
</feature_dependencies>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@project_specs/FRD/F02-uswds-shell-accessibility.md
@project_specs/UX-Mockup/Y0-patterns.md
@project_specs/TechArch/01-components.md

@web/src/screens/CaseDetail.tsx
@e2e/case-detail.spec.ts
@web/src/components/ProvenanceBadge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the header, On-this-page nav, and submitted-entry/findings sections on Carbon</name>
  <files>web/src/screens/CaseDetail.tsx, docs/carbon-conformance-register.md</files>
  <action>
Read the full current `CaseDetail.tsx` before editing (724 lines) to
identify every `usa-*` class in the header, in-page-nav, submitted-entry,
and findings sections specifically (do not touch the recommendation
section or the `<DecisionPanel>`/`<AuditTrailRegion>` render calls in this
task — Task 2 handles the recommendation section, and neither child
component's markup is this plan's concern at all).

Rebuild:
- **Header** (`h1` + status/received/submitted-by + back-to-queue link) →
  Carbon typography + `Link`. Preserve the exact heading text pattern and
  the `tabIndex={-1}` on `h1` (focus-management contract, unchanged).
- **"On this page" navigation** (currently `usa-in-page-navigation`,
  FR-10.10) → rebuild as a plain, unconditional list of native `#fragment`
  anchors to the five matching `<h2 id>` targets, styled with Carbon
  typography/spacing tokens. Carbon has no dedicated in-page-nav component
  equivalent to USWDS's; a plain anchor list is not a "bespoke interactive
  control" (FR-2.1) — it is standard in-page navigation using native `<a
  href="#...">` elements, register it as a documented composition. Preserve
  the fixed reading order (entry → findings → recommendation → decision →
  audit) and that clicking each link genuinely scrolls/focuses its target
  (the existing e2e assertion clicks each link and asserts
  `toBeInViewport()` — keep that working).
- **Submitted-entry section** → rebuild the 14-field display (currently a
  `dl`/`dt`/`dd` or table composition — read the actual current markup) on
  Carbon typography/layout, with each present value's `ProvenanceBadge`
  (HUMAN) rendered via the UNCHANGED import from `web/src/components/
  ProvenanceBadge.tsx` (07-06) — do not alter this file's import statement
  or the badge's usage, only the surrounding layout markup.
- **Findings section** ("Why this case is open") → rebuild the list
  (currently `usa-list` or similar) on Carbon's `UnorderedList`/`ListItem`
  (or plain semantic `<ul>` styled with Carbon tokens if the installed
  `UnorderedList` component fights this content shape) preserving verbatim,
  server-order rendering with NO severity language added (FR-10 unchanged
  requirement).

Update the corresponding `e2e/case-detail.spec.ts` locators for JUST these
sections (header, in-page nav, entry values, findings) — leave any
locator touching the recommendation/decision/audit regions for Task 2 (this
task) or 07-10 (decision/audit) respectively, whichever section actually
owns that assertion; check the spec file's structure to be precise about
section boundaries before editing.

Append rows to `docs/carbon-conformance-register.md` for the header, the
in-page navigation composition, the submitted-entry display, and the
findings list.
  </action>
  <verify>
```bash
npm run build
npm run typecheck
echo "CASEDETAIL HEADER/NAV/ENTRY/FINDINGS OK"
```
  </verify>
  <done>
The header, in-page navigation, submitted-entry, and findings sections of
`CaseDetail.tsx` render entirely on Carbon with the fixed FR-10.10 reading
order intact. Each present entry value still shows its `ProvenanceBadge`
via the unchanged 07-06 import. Build and typecheck are green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the AI-recommendation section (all four presentations) on Carbon</name>
  <files>web/src/screens/CaseDetail.tsx, e2e/case-detail.spec.ts, docs/carbon-conformance-register.md</files>
  <action>
Rebuild the "AI recommendation" section's four presentations:
- **PENDING** (with the 3-second-interval, 60-second-ceiling poll) → the
  polling `setTimeout` logic is UNCHANGED (pure JS timing, no markup
  concern); only the rendered "Generating a recommendation…" presentation
  moves to Carbon (likely reusing the 07-06-rebuilt `Loading`/
  `InlineLoading`, imported unchanged, or a locally-scoped Carbon
  `Loading` instance if the region-scoping differs from the shared
  component's contract — verify against the shared `Loading`'s actual
  region-scoping behavior before duplicating logic that already exists).
- **AVAILABLE** → the "Nothing here has been applied" un-applied-proposal
  framing (Pattern 2 of `Y0-patterns.md`, P2 "proposal framing everywhere")
  stays VERBATIM in wording. Rebuild the per-value AI/HUMAN comparison rows
  (currently a `dl`/`dt`/`dd` composition per TechArch §7.3's register
  entry) on Carbon typography, with EACH row showing both the submitted
  (HUMAN) value with its badge and the AI-suggested value with its badge —
  both via the unchanged `ProvenanceBadge` import. The rationale renders as
  plain paragraphs (no accordion/collapse — FR-10.4 forbids ever collapsing
  rationale).
- **UNAVAILABLE / stale-PENDING** → renders via the 07-06-rebuilt
  `Degraded` component, imported UNCHANGED — this state is explicitly "not
  an error" (UX Pattern 8) and must keep using `Degraded`, never
  `ErrorState`.
- **Model/prompt/generated footnote** → plain Carbon typography, unchanged
  content.

Preserve unchanged: the announce-politely-without-stealing-focus behaviour
on a PENDING→terminal transition; the uuid-to-case-reference canonicalising
`navigate(..., {replace:true})` with no reload/focus-move; the dedicated
"Case not found" presentation; read-only throughout (no mutation control
anywhere in this section, per FR-10.12).

Finish updating `e2e/case-detail.spec.ts`'s remaining locators for the
recommendation section specifically (the `.usa-tag` provenance-badge
selectors used inside THIS section's comparison rows — the decision-region
and audit-region `.usa-tag` selectors elsewhere in this same spec file
belong to 07-10, since those regions are rendered by the still-USWDS
`DecisionPanel`/`AuditTrailRegion` until then; do not touch those specific
assertions in this task).

Append the AI-recommendation comparison-row composition to
`docs/carbon-conformance-register.md`, mirroring the retired register's
own explanatory note about why it is a composition and not a single Carbon
component.
  </action>
  <verify>
```bash
npm run build
npm run typecheck
npx playwright test e2e/case-detail.spec.ts -g "recommendation|entry|findings|On this page|not found|On this page nav" --reporter=list 2>&1 | tail -60
echo "CASEDETAIL RECOMMENDATION SECTION OK"
```
Note: this plan's Playwright run targets ONLY the scenarios this plan's two
tasks actually rebuilt (header/nav/entry/findings/recommendation); any
scenario asserting on the decision or audit-trail region is EXPECTED to
still reference USWDS markup until 07-10 and is explicitly NOT required to
pass yet — record which scenarios were skipped from this run and why in
the SUMMARY, so 07-10 knows exactly what it inherits.
  </verify>
  <done>
The AI-recommendation section's four presentations all render on Carbon,
with per-value provenance badges intact and the un-applied-proposal wording
verbatim. The subset of `e2e/case-detail.spec.ts` scenarios covering
header/nav/entry/findings/recommendation passes. Scenarios covering the
decision or audit-trail region are explicitly deferred to 07-10 and
recorded as such in the SUMMARY — not silently left broken without
explanation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server case/recommendation response → CaseDetail render | Entry values, findings, and recommendation values cross into this screen; all read-only, no mutation path exists in the sections this plan touches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-24 | Information disclosure | Recommendation-comparison rows rendering AI vs HUMAN values | mitigate | Each value renders through the unchanged `ProvenanceBadge` (07-06), preserving the four-carrier colour-independence guarantee; this plan does not alter that import or its usage |
| T-07-25 | Denial of service | The 3s/60s recommendation poll | accept | Timing logic is unchanged by this plan (presentation-only edit); no new risk introduced |
| T-07-26 | Elevation of privilege | Read-only guarantee (FR-10.12) regressing during rebuild | mitigate | Task 2 explicitly re-states "no mutation control anywhere in this section" as a done-criterion; the read-only invariant is additionally guarded by `e2e/case-detail.spec.ts`'s existing inert-controls assertions |

</threat_model>

<verification>
- Header, in-page nav, submitted-entry, findings, and recommendation
  sections all render on Carbon.
- The subset of `e2e/case-detail.spec.ts` covering those sections passes;
  decision/audit-region scenarios are explicitly deferred and documented.
- `ProvenanceBadge` usage is unchanged (imported, not reimplemented here).
- `npm run build` and `npm run typecheck` are green.
</verification>

<success_criteria>
- The largest, most complex screen's read-only reporting surface is fully
  Carbon-rendered.
- 07-10 has a clear, documented handoff of exactly which case-detail
  scenarios remain USWDS-dependent until it runs.
</success_criteria>

<output>
After completion, create `.planning/phases/07-redesign-ui-seeded-demo-data-and-real-llm-integration/07-09-SUMMARY.md`
</output>
</content>
