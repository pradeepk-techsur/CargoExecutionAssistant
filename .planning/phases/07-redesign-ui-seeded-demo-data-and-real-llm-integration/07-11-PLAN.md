---
phase: 07-redesign-ui-seeded-demo-data-and-real-llm-integration
plan: 11
type: execute
wave: 6
depends_on: ["07-07", "07-08", "07-10"]
files_modified:
  - package.json
  - web/styles/app.scss
  - web/styles/_tokens.scss
  - web/scripts/copy-uswds-assets.mjs
  - web/index.html
  - server/test/architecture/navigation.spec.ts
  - server/test/architecture/headers.spec.ts
  - server/test/architecture/absence.spec.ts
  - e2e/whole-loop.spec.ts
  - docs/uswds-conformance-register.md
  - docs/carbon-conformance-register.md
autonomous: true

features:
  implements: ["F2"]
  depends_on: []
  enables: []

must_haves:
  truths:
    - "@uswds/uswds and every USWDS-only build script are fully removed from the dependency tree and the build pipeline — no remnant survives without an explicit, documented reason"
    - "The compiled stylesheet contains ONLY Carbon's cds-- classes; no usa- rule exists in the shipped CSS or any .tsx source"
    - "The three named architecture tests (navigation.spec.ts, headers.spec.ts, absence.spec.ts's raw-hex/px scan) are explicitly re-verified or updated by name against the fully-Carbon codebase, not left to pass by accident"
    - "docs/uswds-conformance-register.md is finalized with its superseding pointer to docs/carbon-conformance-register.md, which is itself complete for every product control"
    - "The full regression gate — npm run test AND npm run test:e2e — is green with zero failures and zero skips, as the mandatory final proof the Carbon migration shipped without regression"
  artifacts:
    - path: "package.json"
      provides: "No @uswds/uswds dependency; build:css/build:assets scripts reference only the Carbon pipeline"
    - path: "docs/carbon-conformance-register.md"
      provides: "The complete, final Carbon conformance register for every interactive control in the product"
  key_links:
    - from: "web/index.html"
      to: "the compiled Carbon-only stylesheet"
      via: "<link rel=\"stylesheet\" href=\"...\">, no uswds.min.js script tag remaining"
      pattern: "link rel=\"stylesheet\""

integration_contracts:
  requires:
    - from_plan: "07-10"
      artifact: "docs/a11y/case-detail.md"
      exports: ["joint five-section sign-off"]
      verify: "grep -qi 'all five sections' docs/a11y/case-detail.md && echo CONTRACT_OK"
    - from_plan: "07-07"
      artifact: "web/src/screens/SignIn.tsx"
      exports: ["SignIn"]
      verify: "grep -q 'export function SignIn' web/src/screens/SignIn.tsx && echo CONTRACT_OK"
    - from_plan: "07-08"
      artifact: "web/src/screens/Queue.tsx"
      exports: ["Queue"]
      verify: "grep -q 'export function Queue' web/src/screens/Queue.tsx && echo CONTRACT_OK"
  provides:
    - artifact: "package.json"
      exports: ["no @uswds/uswds dependency"]
      shape: "dependencies block with @uswds/uswds absent"
      verify: "! grep -q '\"@uswds/uswds\"' package.json && echo CONTRACT_OK"
    - artifact: "docs/carbon-conformance-register.md"
      exports: ["complete product-wide Carbon control register"]
      shape: "Markdown table covering every control across shell + all six screens"
      verify: "test -f docs/carbon-conformance-register.md && grep -qi 'audit' docs/carbon-conformance-register.md && echo CONTRACT_OK"
---

<objective>
Close out the Carbon migration: remove `@uswds/uswds` and every USWDS-only
build artefact from the repository entirely (now that every one of the
eighteen originally USWDS-coupled files renders on Carbon, per 07-05 through
07-10), explicitly re-verify the three architecture tests the quality gate
names by number, finalize the Carbon conformance register, and run the
mandatory full regression gate — `npm run test` AND `npm run test:e2e` —
proving the whole redesign shipped with zero functional or accessibility
regression.

This is the ONLY plan in the phase permitted to delete USWDS entirely,
because it is the first point at which nothing in the codebase still
depends on it.

Purpose: leave the repository in a state where Carbon is the ONLY design
system present, with no dead dependency, no dead build step, and a fully
green regression suite as the closing proof.
Output: `@uswds/uswds` removed from `package.json`; `app.scss`'s USWDS
forward and `copy-uswds-assets.mjs` removed; the three named architecture
tests explicitly re-verified/updated; the conformance register finalized;
`npm run test && npm run test:e2e` green.
</objective>

<feature_dependencies>
Implements: F2: USWDS Application Shell & Accessibility Foundation — this
plan is where the "USWDS Application" half of F2's name stops being true of
the codebase, closing out the Phase 7 redesign.
Depends on: None as a tracked feature ID.
Enables: None as a tracked feature ID.
</feature_dependencies>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@project_specs/FRD/F02-uswds-shell-accessibility.md
@project_specs/TechArch/06-tech-stack.md
@project_specs/TechArch/07-accessibility.md

@package.json
@web/styles/app.scss
@web/styles/_tokens.scss
@web/scripts/copy-uswds-assets.mjs
@web/index.html
@server/test/architecture/navigation.spec.ts
@server/test/architecture/headers.spec.ts
@server/test/architecture/absence.spec.ts
@docs/uswds-conformance-register.md
@docs/carbon-conformance-register.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove USWDS from the dependency tree and build pipeline entirely</name>
  <files>package.json, web/styles/app.scss, web/styles/_tokens.scss, web/scripts/copy-uswds-assets.mjs, web/index.html</files>
  <action>
Confirm (do not assume) that no `.tsx` file under `web/src` still contains
a `usa-`/`uswds` reference:
```bash
grep -rl "usa-\|uswds" web/src --include="*.tsx" | sort
```
This MUST return empty (or only files with an incidental, non-visual
reference you must individually justify in the SUMMARY — e.g. a code
comment mentioning the historical USWDS name is acceptable; a live
`className="usa-*"` is not). If it is non-empty, STOP and fix the
remaining file(s) first — do not remove the USWDS stylesheet out from
under markup that still expects it.

Once confirmed clean:
- Remove `@uswds/uswds` from `package.json`'s `dependencies`
  (`npm uninstall @uswds/uswds`).
- In `web/styles/app.scss`: remove the `@use "uswds-core" with (...)` block
  and the `@forward "uswds";` line entirely. Keep the Carbon forward (07-04)
  and the reduced-motion media query (design-system-agnostic, unchanged).
  Delete `web/styles/_tokens.scss` and its forward/use statement — 07-04
  already marked it superseded; this is where that superseded seam is
  actually removed, since nothing could conceivably still reference it now.
- Delete `web/scripts/copy-uswds-assets.mjs` and its invocation in
  `package.json`'s `build:assets` script (keep `copy-carbon-assets.mjs`,
  from 07-04, as the sole asset-copy step).
- In `web/index.html`: remove the `<script type="module" src="/assets/js/
  uswds.min.js"></script>` tag (no component depends on USWDS's JS anymore
  — the banner's disclosure is now the shell's own React state, per 07-05).
  Rename the compiled stylesheet reference from `/assets/uswds.css` to a
  name that no longer implies USWDS (e.g. `/assets/app.css`) — update BOTH
  the `<link>` tag here AND the `build:css` script's output path in
  `package.json` to match, and grep the repository for any other reference
  to the old filename (`e2e/shell.spec.ts`'s stylesheet-reachability check,
  named in that plan, is the one this project already knows about — update
  it here too).

Confirm the compiled stylesheet now contains ONLY `cds--*` rules:
```bash
npm run build:css
grep -c "usa-banner\|usa-form\|usa-table" web/public/assets/app.css   # expect 0
grep -c -- "cds--" web/public/assets/app.css                          # expect > 0
```
  </action>
  <verify>
```bash
grep -rl "usa-\|uswds" web/src --include="*.tsx" | sort   # expect EMPTY
! grep -q '"@uswds/uswds"' package.json
npm run build
ls web/public/assets/app.css
grep -c "usa-" web/public/assets/app.css || true
echo "USWDS REMOVED"
```
  </verify>
  <done>
`@uswds/uswds` is absent from `package.json`. `web/styles/app.scss` forwards
only Carbon. `web/styles/_tokens.scss` and `web/scripts/copy-uswds-
assets.mjs` are deleted. `web/index.html` references only the renamed
Carbon-only stylesheet and no longer loads `uswds.min.js`. The compiled CSS
contains zero `usa-*` rules and only `cds--*` rules. `npm run build`
succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Explicitly re-verify the three named architecture tests; finalize the register; run the full regression gate</name>
  <files>server/test/architecture/navigation.spec.ts, server/test/architecture/headers.spec.ts, server/test/architecture/absence.spec.ts, e2e/whole-loop.spec.ts, docs/uswds-conformance-register.md, docs/carbon-conformance-register.md</files>
  <action>
**`navigation.spec.ts`.** Run it against the fully-Carbon shell and confirm
every assertion still passes UNMODIFIED (NAV_ITEMS count/shape, rendered
`<nav>` anchor count, the seven-route table, the excluded-affordance scan,
the pinned seven-link footer set, the no-RBAC-identifier scan, the reduced-
shell-has-no-nav assertion). If any assertion required a code change to
keep passing (for example, if Carbon's `HeaderNavigation` renders an extra
wrapping `<a>` that shifts an anchor count), make the MINIMAL corresponding
test update and record exactly what changed and why in the SUMMARY — do
not weaken an assertion's INTENT to make it pass.

**`headers.spec.ts`.** Run it and confirm the D-1 no-frame-blocking-header
behavioural/source guarantees, the CSP `frame-ancestors` contract, the
`dangerouslySetInnerHTML` scan, and the template-literal-SQL scan all still
pass. This file has no direct USWDS dependency, so it should pass
unmodified — explicitly confirm this rather than assuming, since the quality
gate requires this test be named and checked, not silently trusted.

**`absence.spec.ts`'s raw-hex/px scan.** Run
`no raw hex colour or raw px value in web/src/**/*.tsx` specifically and
confirm zero offenders across the now-fully-Carbon `web/src` tree (Carbon
components take token props, not raw values, so this should already hold
from every prior plan's own verification — confirm it holds for the WHOLE
tree at once here, not just file-by-file as each prior plan checked its own
diff). Also confirm the CDN-absence scan and the dependency-allowlist tests
(`does not depend on <name>` for each forbidden package) still pass with
`@uswds/uswds` gone and `@carbon/react`/`@carbon/icons-react` present (none
of the forbidden-dependency names collide with Carbon's package names).

**Finalize the conformance register.** Review `docs/carbon-conformance-
register.md` end to end against the actual shipped product — confirm every
row from the retired `docs/uswds-conformance-register.md` has a
corresponding Carbon-mapped row (shell, sign-in, queue, case-detail entry/
findings/recommendation, decision, audit), add any row a prior plan missed,
and add a closing "How to extend this register" section mirroring the
retired file's own convention (append-only from here forward). Add one
final line to `docs/uswds-conformance-register.md` (its LAST line, do not
edit anything above it) confirming: "Phase 7 complete — `@uswds/uswds` is
no longer a dependency of this repository as of this commit;
`docs/carbon-conformance-register.md` is now the current register."

**Update `e2e/whole-loop.spec.ts`** (the end-to-end keyboard-only walkthrough
spanning sign-in → entry → queue → case → decision → audit in one
unbroken session) for any remaining class-name-based locator that the
per-screen plans' updates did not already cover — this file spans every
screen, so it is the one place a missed selector update would surface.

**Run the mandatory full regression gate** — this is the phase's final
proof:
```bash
npm run test       # unit + db + api + arch
npm run test:e2e   # the full Playwright suite, every spec
```
Both MUST be green, 0 failures, 0 skipped. If anything fails, fix it in
this plan (this is the last wave — there is no later plan to defer to).
  </action>
  <verify>
```bash
npx vitest run server/test/architecture/navigation.spec.ts server/test/architecture/headers.spec.ts server/test/architecture/absence.spec.ts --reporter=default
npm run test
npm run test:e2e
echo "PHASE 7 CARBON MIGRATION — FULL REGRESSION GREEN"
```
  </verify>
  <done>
`navigation.spec.ts`, `headers.spec.ts`, and `absence.spec.ts`'s raw-hex/px
scan are each explicitly confirmed passing (or minimally, documentedly
updated) against the fully-Carbon codebase. `docs/carbon-conformance-
register.md` is complete for every product control; `docs/uswds-
conformance-register.md` carries its final superseding line. `npm run test`
(unit/db/api/arch) and `npm run test:e2e` (the full Playwright suite) are
BOTH green — 0 failures, 0 skipped — proving the Carbon migration shipped
with no regression to any F2 guarantee.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → shipped artifact | The final compiled bundle (CSS, JS, fonts) is what actually reaches the browser; this plan is the last point where a stray USWDS remnant or an incomplete architecture-test update could silently ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-30 | Tampering / scope regression | A stray `usa-*` class or `@uswds/uswds` remnant surviving removal | mitigate | Task 1's verify step greps the ENTIRE `web/src` tree and the compiled CSS for `usa-`/`uswds`, failing loudly if anything remains, rather than trusting each prior plan's own scoped diff |
| T-07-31 | Denial of service / scope creep | Architecture-test weakening to force a pass after the Carbon rebuild | mitigate | Task 2 explicitly requires that any necessary test update preserve the assertion's INTENT (documented in the SUMMARY), not merely make it pass — the same standard `navigation.spec.ts`'s own file header states for itself |
| T-07-32 | Denial of service | Full regression suite (`npm run test:e2e`) revealing a cross-screen integration gap invisible to any single prior plan | mitigate | This is precisely why the full gate is reserved for this final wave rather than run incrementally — `whole-loop.spec.ts` is the one spec that exercises every screen boundary in one session, and this plan's own `<done>` requires it green before the phase can close |

</threat_model>

<verification>
- `grep -rl "usa-\|uswds" web/src --include="*.tsx"` returns empty.
- `@uswds/uswds` is absent from `package.json`; the compiled stylesheet
  contains zero `usa-*` rules.
- `navigation.spec.ts`, `headers.spec.ts`, and `absence.spec.ts`'s raw-hex/
  px scan are each explicitly re-verified (named, not assumed).
- `docs/carbon-conformance-register.md` is complete; `docs/uswds-
  conformance-register.md` carries its final superseding note.
- `npm run test` AND `npm run test:e2e` are BOTH green, 0 failures, 0
  skipped.
</verification>

<success_criteria>
- The Carbon Design System is the ONLY visual system in the repository.
- Every F2 structural/accessibility guarantee is proven intact by a green,
  full regression suite — not by inspection alone.
- The phase's redesign goal ("a future visual redesign is a swap, not a
  rewrite" from 07-03's premise) is realized: the swap happened, cleanly,
  in reviewable, dependency-ordered waves, with nothing left half-migrated.
</success_criteria>

<output>
After completion, create `.planning/phases/07-redesign-ui-seeded-demo-data-and-real-llm-integration/07-11-SUMMARY.md`
</output>
</content>
