---
phase: 05-ai-recommendation-as-an-un-applied-proposal
plan: 06
type: execute
wave: 4
depends_on: ["05-05"]
files_modified:
  - e2e/env.ts
  - e2e/case-detail.spec.ts
  - docs/a11y/case-detail.md
  - docs/uswds-conformance-register.md
autonomous: true

features:
  implements: ["F10"]
  depends_on: ["F9"]
  enables: []

must_haves:
  truths:
    - "A real, unmocked entry that opens an exception shows its AI recommendation becoming available on the case screen within the FR-10.7 polling budget, with no reload and no focus movement"
    - "With the FakeProvider forced into each failure branch, the case screen shows a stated 'no recommendation available' condition, never an error state and never an unbounded spinner"
    - "Every AI-suggested value and every specialist-entered value is announced distinctly to assistive technology, and the distinction survives a colour-removed (monochrome) rendering"
    - "The heading order h1 -> h2s in normative order is proven in a real browser, with the six h2 sections present even where their content is a stub"
    - "An 'On this page' in-page navigation list, placed after the header, contains one link per h2 section and each link, when activated, genuinely resolves to its target heading (FR-10.10)"
    - "npm run test:all is fully green: every unit/db/api/architecture/e2e suite across Phases 1-5 passes with 0 failures and 0 skipped"
  artifacts:
    - path: "e2e/case-detail.spec.ts"
      provides: "the F9/F10 Playwright regression suite"
    - path: "docs/a11y/case-detail.md"
      provides: "the NFR-2 signed accessibility review record for /cases/:caseReference"
  key_links:
    - from: "e2e/case-detail.spec.ts"
      to: "e2e/env.ts"
      via: "AI_PROVIDER_URL=fake:deterministic wired into the shared webServer env"
      pattern: "fake:deterministic"

integration_contracts:
  requires:
    - from_plan: "05-05"
      artifact: "web/src/screens/CaseDetail.tsx"
      exports: ["CaseDetail"]
      verify: "grep -n 'export function CaseDetail' web/src/screens/CaseDetail.tsx && echo CONTRACT_OK"
    - from_plan: "05-04"
      artifact: "server/src/index.ts"
      exports: ["dispatchRecommendation wired, fake:deterministic provider selection"]
      verify: "grep -n \"config.aiProviderUrl === 'fake:deterministic'\" server/src/index.ts && echo CONTRACT_OK"
  provides:
    - artifact: "e2e/env.ts"
      exports: ["E2E_ENV (with AI_PROVIDER_URL, AI_MODEL_ID, PROMPT_VERSION, AI_TIMEOUT_MS, AI_WORKER_CONCURRENCY)"]
      shape: "AI_PROVIDER_URL: 'fake:deterministic'"
      verify: "grep -n \"AI_PROVIDER_URL\" e2e/env.ts && echo CONTRACT_OK"
---

<objective>
Prove F9 and F10 in a real browser against a real running server — genuine
PENDING → AVAILABLE and PENDING → UNAVAILABLE transitions (via the
`fake:deterministic` provider wired in plan 05-04, so no real network
dependency exists anywhere in the automated suite), a full keyboard/heading-
order/colour-independence walkthrough, and the signed NFR-2 accessibility
record. This is the last plan in the phase: it also runs and confirms the full
`npm run test:all` gate.

Purpose: unit/db/api tests proved the mechanism and the API contract in
isolation; this plan is the only place that proves a specialist, in an actual
browser, actually sees the recommendation appear without reloading and without
losing her place — the phase's own success criteria 1, 2 and 4 in their final,
observable form.
Output: `e2e/case-detail.spec.ts`, the `e2e/env.ts` AI-environment additions,
`docs/a11y/case-detail.md`, the conformance register update, and a fully green
`npm run test:all`.
</objective>

<feature_dependencies>
Implements: F10: Exception Case Detail & Recommendation Presentation UI — the browser-level and accessibility proof
Depends on: F9 (the real, wired generation mechanism this suite exercises end to end)
Enables: None (last plan in the phase)
</feature_dependencies>

<context>
@.planning/STATE.md
@e2e/env.ts
@e2e/sign-in.spec.ts
@e2e/queue.spec.ts
@e2e/shell.spec.ts
@docs/a11y/queue.md
@docs/a11y/sign-in.md
@docs/uswds-conformance-register.md
@web/src/screens/CaseDetail.tsx
@web/src/components/ProvenanceBadge.tsx
@server/src/ai/fakeProvider.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the AI environment into e2e, and the Playwright case-detail suite</name>
  <files>e2e/env.ts,e2e/case-detail.spec.ts</files>
  <action>
**`e2e/env.ts`** — add, alongside the existing keys (the whole object is
handed to the webServer process as its complete environment; a partial
addition would leave the AI self-checks unmet and abort boot per plan 05-01's
new required keys):
```ts
AI_PROVIDER_URL: 'fake:deterministic',
AI_MODEL_ID: 'e2e-fake-model',
PROMPT_VERSION: '2026.09.1', // MUST match the version shipped in plan 05-01
AI_TIMEOUT_MS: '20000',
AI_WORKER_CONCURRENCY: '2',
```
No `AI_API_KEY` needed (config.ts only requires it for a real `https://`
provider). Confirm `PROMPT_VERSION` here is byte-identical to whatever prompt
version plan 05-01 actually shipped — if it named the template file
differently, use that name instead of `2026.09.1`.

**Create `e2e/case-detail.spec.ts`**, following `e2e/queue.spec.ts`'s
conventions (`E2E_ENV`, real sign-in, `BASE = 'http://127.0.0.1:3000'`, mocked
`page.route()` for scenarios needing a KNOWN data shape, real API-created
fixtures for genuine end-to-end proof). `FAKE_AI_TRIGGERS`' marker strings
(from `server/src/ai/fakeProvider.ts` — import the same constant or hard-code
the exact literal strings, documenting which) can be placed in
`goods_description` when creating a test entry via `request.post('/api/entries', {...})`
to deterministically force a chosen failure branch. Cover:

1. **Real end-to-end AVAILABLE (phase success criteria 1 & 3):** sign in,
   `POST /api/entries` with a body that fails validation but carries NO
   `FAKE_AI_TRIGGERS` marker (so FakeProvider's default success path fires),
   capture the case reference, navigate to `/cases/{caseReference}`. Assert
   the "AI recommendation" section initially shows the PENDING/"Generating an
   AI recommendation…" presentation (or, if generation is fast enough to have
   already resolved by the time the page loads, the AVAILABLE presentation
   directly — assert whichever is actually observed, then poll/wait up to 10s
   for the AVAILABLE presentation: `recommended_action` text, the full
   rationale, at least one comparison row with BOTH a "Specialist-entered" (if
   the field was submitted) or absent, and an "AI-suggested" badge, and the
   model/prompt/generated-at footnote). Assert `document.activeElement`
   NEVER becomes any element inside the recommendation section during the
   transition (capture the active element's id/role before triggering the
   wait and confirm it is unchanged after — proving FR-10.7's "focus MUST NOT
   be stolen").
2. **Real end-to-end UNAVAILABLE (phase success criterion 5):** create an
   entry whose `goods_description` carries the `PROVIDER_UNAVAILABLE` (or any
   one) `FAKE_AI_TRIGGERS` marker; navigate to its case; wait for the
   recommendation section to reach the UNAVAILABLE presentation; assert the
   `<h3>No AI recommendation available</h3>` heading, the exact mapped cause
   sentence for that `failure_reason`, the "you can still resolve or reject"
   sentence, and the ABSENCE of any Retry/Regenerate control. Assert the
   section uses `role="status"` (the shared `Degraded` component), never
   `role="alert"` (FR-10.8 — this is not an error).
3. **Provenance is textual, not colour-only:** on the AVAILABLE case from
   test 1, inject a stylesheet override neutralising colour (e.g.
   `page.addStyleTag({ content: '* { color: black !important; background: white !important; border-color: black !important; filter: grayscale(100%) !important; }' })`)
   BEFORE re-reading the page, and assert the accessible name / visible text
   of every `ProvenanceBadge` instance still reads "AI-suggested" or
   "Specialist-entered" — i.e. the distinction is provable from text content
   alone, independent of any colour assertion.
4. **Heading order (FR-10.10, phase success criterion 1):** on the same case,
   collect every heading element in DOM order via
   `page.locator('h1, h2, h3').allTextContents()` (or role-based
   `getByRole('heading')`) and assert: exactly one `h1` (first), followed by
   h2s in the order "Why this case is open", "Submitted entry", "AI
   recommendation", "Your decision", "Audit trail" (any `h3`s — "Why the AI
   suggests this", "No AI recommendation available" — appear strictly between
   their owning h2 and the next h2).
5. **Findings and entry values render as submitted (FR-10.6, FR-10.16):**
   assert the "Why this case is open" list item text matches the finding
   message the API returned (fetch `/api/exceptions/{id}` directly via
   `request.get` in the test to get the authoritative findings and compare),
   and that a submitted field value renders verbatim (no HTML interpretation
   — assert via `.textContent()` not `.innerHTML()`).
6. **uuid canonicalisation (FR-10.13):** navigate directly to
   `/cases/{exceptionUuid}` (the raw uuid, captured from the POST /api/entries
   response's `exception.id`); assert the address bar becomes
   `/cases/{caseReference}` (via `page.waitForURL` or re-reading `page.url()`)
   WITHOUT a full navigation (assert no `load` event fires a second time —
   e.g. inject a `window.__navCount` counter via `page.addInitScript` before
   navigating and confirm it stays at 1).
7. **Decision / audit-trail stubs present, inert (FR-10.9/F14 scope note):**
   assert both h2 headings exist and each stub's alert text is visible; assert
   NO `<button>`/`<select>`/`<textarea>` exists inside either section
   (`page.locator('#audit-trail ~ * button, ...')` or scope by DOM position —
   whatever locator strategy is reliable given the actual markup; a coarse
   "zero buttons/selects/textareas anywhere below the AI-recommendation
   section other than any inside ErrorState" check is acceptable).
8. **Case-not-found (FR-10.15):** navigate to `/cases/CE-2099-999999` (a
   well-formed but certainly-unmatched reference); assert the "Case not
   found" heading and a working "Back to review queue" link — distinct from a
   generic error state (no "Try again" button here).
9. **Keyboard traversal:** from the queue (test 1's entry point), Tab through
   the case screen and confirm the "Back to review queue" link, any comparison
   row content, and (once built, Phase 6) no decision control trap focus —
   for Phase 5, simply confirm Tab order reaches "Back to review queue" and
   then the end of the document without any element requiring more than one
   Tab to reach (no `tabindex` > 0 — reuse `shell.spec.ts`'s existing
   "no element has tabindex greater than 0" pattern by navigating it to this
   route, or add an equivalent assertion here).
10. **"On this page" in-page navigation (FR-10.10 — this revision's blocker
    fix, plan 05-05's structural side, this test's browser-level proof):** on
    the case from test 1, locate the `nav` landmark named "On this page"
    (`page.getByRole('navigation', { name: 'On this page' })`) placed
    immediately after the header and BEFORE the "Why this case is open"
    heading (assert its DOM position precedes that heading's). Assert it
    contains exactly five links, in order, with accessible names "Why this
    case is open", "Submitted entry", "AI recommendation", "Your decision",
    "Audit trail". For EACH of the five links: click it, then assert
    `page.url()` ends with the matching `#why-open` / `#submitted-entry` /
    `#ai-recommendation` / `#your-decision` / `#audit-trail` fragment AND that
    the corresponding heading (`page.locator('#<id>')`) is scrolled into the
    viewport (`toBeInViewport()` or an equivalent bounding-box check) —
    proving each link genuinely resolves to its target heading, not merely
    that the href string looks right.
  </action>
  <verify>
npm run build 2>&1 | tail -30 && echo "BUILD OK"
npx playwright test e2e/case-detail.spec.ts --reporter=list 2>&1 | tail -80 && echo "PLAYWRIGHT PASSED"
  </verify>
  <done>`e2e/case-detail.spec.ts` exists with all 9 scenarios above; every test passes (0 failing, 0 skipped); the e2e webServer boots successfully with the new AI environment keys.</done>
</task>

<task type="auto">
  <name>Task 2: The NFR-2 accessibility sign-off, the conformance register update, and full phase verification</name>
  <files>docs/a11y/case-detail.md,docs/uswds-conformance-register.md</files>
  <action>
**`docs/uswds-conformance-register.md`** — append rows (append-only, never
rewrite existing rows) for the new controls this phase introduced:
- `ProvenanceBadge` ("AI-suggested" / "Specialist-entered") — its USWDS basis
  (`usa-tag` + a USWDS icon via the sprite) — screen: Case detail — record:
  `docs/a11y/case-detail.md`.
- The AI-recommendation comparison row composition — note it as a composition
  (definition-list row + two `ProvenanceBadge`s + plain text), same pattern as
  the register's existing "Error summary" composition note.
- `Degraded` reused for the UNAVAILABLE recommendation presentation — note
  this is the SAME component already registered for Phase 4/prior contexts;
  add a screen reference to Case detail if the existing row doesn't already
  cover it, otherwise skip (do not duplicate a row for an already-registered
  shared component — check the existing "Shared status/error/empty/degraded/
  read-only states" row first).
Add a short paragraph under **Notes** for the comparison-row composition,
mirroring the Error-summary note's structure.

**`docs/a11y/case-detail.md`** — follow `docs/a11y/queue.md`'s EXACT
structure (header table, Evidence set with REAL numbers from this phase's test
run, §7.7/Y2 §12 checklist with each line citing the specific
`case-detail.spec.ts` test or architecture assertion that proves it, Defects
table, Assistive-technology walkthrough narrative, Sign-off). Run the full
evidence set FIRST:
```bash
npm run test 2>&1 | tail -30
npx playwright test --reporter=list 2>&1 | tail -60
```
Cite the real pass counts (unit/db/api/arch/e2e totals) — do not copy
`queue.md`'s numbers verbatim; they will have grown across Phase 5's plans.

Checklist lines to include, each citing a real test:
- Single `<h1>` "Case {reference}"; h2s descend in normative order with no
  skipped level → Task 1 test 4.
- An "On this page" in-page navigation list appears after the header, before
  "Why this case is open", with one correctly-resolving link per h2 section
  (FR-10.10) → Task 1 test 10.
- Every AI-suggested and specialist-entered value carries text+icon
  provenance, not colour alone → Task 1 test 3, plus a screen-reader
  walkthrough line confirming each `ProvenanceBadge`'s accessible name is
  announced.
- The AI's proposal framing is present verbatim ("The AI suggests:", "Nothing
  here has been applied...") and the forbidden words ("applied", "fixed",
  "auto-resolved", "corrected", "updated") appear nowhere on the screen —
  cite a grep-style assertion if you add one to the e2e suite, or mark this
  `[pass]` "confirmed by reviewer" if only manually checked.
- Rationale renders in full, as plain text, preserving paragraph breaks, never
  truncated → Task 1 test 1 (renders `recommended_action` and rationale text
  in full).
- PENDING presentation is `aria-busy`, announces politely, and updates in
  place on a terminal transition WITHOUT moving focus → Task 1 test 1's focus
  assertion.
- UNAVAILABLE presentation uses `Degraded` (`role="status"`, never
  `role="alert"`), states a plain cause, offers no Retry control → Task 1
  test 2.
- Case-not-found is distinguished from a generic error and offers a working
  path back to the queue → Task 1 test 8.
- uuid URLs canonicalise without a full page reload → Task 1 test 6.
- No mutation control anywhere on the screen (decision/audit-trail sections
  are inert stubs) → Task 1 test 7.
- Keyboard reachability, no `tabindex` > 0 → Task 1 test 9 /
  `shell.spec.ts`'s existing pattern.
- AA contrast, colour independence, `prefers-reduced-motion`, live-region
  correctness → mark `[pass]` "confirmed by reviewer" (same convention as
  `queue.md`), noting this screen introduces exactly one new visual element
  (`ProvenanceBadge`) reviewed here for the first time.
- ASSISTIVE-TECHNOLOGY WALKTHROUGH: the header, findings list, submitted-entry
  definition list (each value's provenance announced), the recommendation
  section in each of its states, and the decision/audit-trail stub headings —
  all announced correctly, nothing announced twice, nothing silently skipped.

Sign-off section: same format as `queue.md`.

**Finish with the PHASE-level gate** (this is the last plan in the phase):
```bash
npm run build 2>&1 | tail -30
npm run typecheck 2>&1 | tail -20
npm run test:all 2>&1 | tail -100
```
All must exit 0 / show 0 failures, 0 skipped, across every tier (unit, db,
api, architecture, e2e) accumulated over Phases 1–5.
  </action>
  <verify>
npm run test:all 2>&1 | tail -100 && echo "PHASE GATE OK"
  </verify>
  <done>`docs/a11y/case-detail.md` exists, signed, every checklist line citing a real passing test; `docs/uswds-conformance-register.md` has new append-only rows; `npm run test:all` passes with 0 failures and 0 skipped across the whole project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| test-harness→server | The e2e webServer now boots with `AI_PROVIDER_URL=fake:deterministic` — a deliberately non-production posture that must never be reachable from a real deployment's environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-05-16 | Tampering | `AI_PROVIDER_URL=fake:deterministic` accidentally shipped to a real deployment | accept | This value is set only in `e2e/env.ts` (test-only) and would need to be deliberately set in a real deployment's own environment/compose file to take effect; `config.ts`'s self-check (plan 05-01) requires EITHER this exact literal OR a real `https://` URL, so a typo'd or partial value fails loudly at boot rather than silently defaulting to the fake provider. Owner: deployment configuration, outside this codebase's control — the same residual-risk shape as every other environment-variable-driven posture (`SESSION_COOKIE_PROFILE=demo-iframe`). |

</threat_model>

<verification>
```bash
npm run build 2>&1 | tail -30
npm run typecheck 2>&1 | tail -20
npm run test:all 2>&1 | tail -100
```
All exit 0. This is the LAST plan in the phase — `npm run test:all` is the
phase-completion gate and must be fully green.
</verification>

<success_criteria>
- The real generation mechanism is proven end-to-end in a browser, for both the AVAILABLE and each UNAVAILABLE outcome, with no real network dependency.
- Provenance is proven distinguishable by text/shape alone (monochrome).
- Heading order, uuid canonicalisation, case-not-found, and the inert decision/audit-trail stubs are all proven in a real browser.
- `docs/a11y/case-detail.md` is signed off per NFR-2.
- `npm run test:all` passes with 0 failures, 0 skipped — the phase is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-recommendation-as-an-un-applied-proposal/05-06-SUMMARY.md`
</output>
